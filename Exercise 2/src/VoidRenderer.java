/**
 * A minimal implementation of the Renderer interface that performs no rendering.
 */
public class VoidRenderer implements Renderer{
    /**
     * Does not perform any rendering for the provided game board.
     *
     * @param board The game board that does not require rendering.
     */
    public void renderBoard(Board board){}
}
